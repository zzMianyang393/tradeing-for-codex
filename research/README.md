# Research workspace

研究脚本目前仍保留在仓库根目录，以避免移动文件破坏现有 import 和历史命令。

新的研究代码优先放入这里，并满足：

- 固定 BTC/ETH 或预注册固定宇宙
- 默认 10U、含成本
- 有独立验证入口和 Markdown 研究卡
- 结论进入策略注册表前需要人工确认

历史根目录研究脚本会在后续阶段按依赖关系逐步迁移；本阶段先保证 Hermes 可以完整拉取并复现现有研究环境。
